// 该文件用于全局状态管理，等待项目复杂的时候，可以将state,mutations分割出去
import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  // 全局状态
  state: {
  },
  // 改变状态的行为
  mutations: {
  },
  // 异步改变状态的行为
  actions: {
  },
  // 模块
  modules: {
  }
})
